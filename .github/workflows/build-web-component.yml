name: Update web components files

on: 
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs: 
  clone-and-build-web-component:
    runs-on: ubuntu-latest
    steps:
      - name: Starting
        run: echo "Hello I'm working"
